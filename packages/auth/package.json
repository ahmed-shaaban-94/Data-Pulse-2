{
  "name": "@data-pulse-2/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Shared session/token primitives used by both api and worker. Skeleton only — argon2id + token-hash helpers land in a later branch."
}

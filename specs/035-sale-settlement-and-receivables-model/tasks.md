# Tasks: Sale Settlement & Receivables Model

**Feature**: `035-sale-settlement-and-receivables-model` | **Date**: 2026-06-15
**Spec**: [spec.md](./spec.md) | **Plan**: [plan.md](./plan.md)

> **Task posture — GATED BOOKKEEPING (read first).**
> This `tasks.md` is **intentional gated bookkeeping**, not a build-ready task list.
> Hand-authored (not generated by `/speckit-tasks`) because the substrate a normal
> task decomposition consumes — `data-model.md` + `contracts/` — is deliberately
> **absent (gated)**; there is nothing concrete to decompose into file-level tasks.
> Every task below is held at **gate level** and pre-commits **no** table name,
> endpoint, field shape, or migration path — the same OQ-4 / OQ-7 / G2 / G3 line the
> plan holds. The list **unblocks nothing**; it records the sequence and the hard
> blockers so the owner sees exactly what gates what. **No task here is executable
> until its blocking gate resolves.** Uncommitted; for review.

## Blocking legend

- `[BLOCKED: OQ-4]` — needs DP-026 returns/reversal determination CLOSED + reversal
  technical-compatibility confirmed (DP-026 is itself an OPEN determination today).
- `[BLOCKED: OQ-7]` — needs an explicit **Payment Entry ownership** ruling
  (DP-2 vs ERPNext-via-connector); no signed decision/constitution principle settles it.
- `[BLOCKED: G2]` — needs the OpenAPI contract slice authored + owner both-sides
  approval (the producer's eventual output).
- `[BLOCKED: G3]` — needs the conceptual model promoted to a `[GATED]` schema/migration.
- `[BLOCKED: G6]` — tax activation under ADR-0003 (VAT allocation); deferred (OQ-2).
- `[OWNER]` — a governance/decision act, not a code task; only the owner can clear it.
- `[READY]` — executable now without resolving any gate.

---

## Phase A — Gate resolution (OWNER / cross-system; clears everything downstream)

- [x] **T001** `[RESOLVED: CARVE]` ~~Close DP-026 determination~~ → **CARVE ruling
  (035-DR-SETTLEMENT §OQ-4):** non-reversal happy path proceeds now; **reversal-
  compatibility work (FR-024) stays BLOCKED pending DP-026's close** (later additive
  bump). Sequencing decision, **not** a compat answer. No parallel reversal model
  (NG-1). DP-026 §3.x anchor mismatch flagged to 026 CHECKPOINT-2 (informational).
- [x] **T002** `[RESOLVED: 7-C]` **Payment Entry ownership ruled 7-C**
  (035-DR-SETTLEMENT §OQ-7): DP-2 owns operational receivable + cash-application
  truth; ERPNext owns the accounting Payment Entry as a reconciled valuation
  projection; connector is the only adapter. Shapes 035 fields; ERPNext posting stays
  gated behind 011-DR-POSTING-R1.
- [x] **T003** `[RESOLVED: tax-off v1]` **Tax-deactivated v1** (035-DR-SETTLEMENT
  §OQ-2): no VAT allocation / fiscal-receipt / co-pay split / activation; placeholders
  only (FR-023). G6 reopens later with SME review.

> **T001 (CARVE) + T002 (7-C) cleared → the NON-REVERSAL G2 contract DRAFT is
> authorable (T010a). Reversal fields remain blocked on DP-026. OpenAPI YAML
> authoring (T010b) + schema (T020) stay `[GATED]`.**

---

## Phase B — G2 contract slice `[GATED]` (the gate that unblocks all 5 children)

- [~] **T010a** `[READY]` **Author the G2 contract DRAFT (markdown design input, NOT
  OpenAPI YAML, NOT under `packages/contracts/**`)** — see
  [`g2-contract-draft.md`](./g2-contract-draft.md). Non-reversal surface: payer
  account, receivable + lifecycle, settlement command/outcome, payment/cash-application
  (per 7-C), claim, remittance/reconciliation. Reversal fields **excluded** (carve).
  *In progress this session.*
- [ ] **T010b** `[BLOCKED: G2]` `[OWNER]` Transcribe the approved draft into the
  **OpenAPI contract** — `[GATED]` path `packages/contracts/openapi/**`. Needs owner
  dispatch; not authored here.
- [ ] **T011** `[BLOCKED: G2]` Pin idempotency-key + envelope conventions on every write
  operation in the contract (per spec FR-020; Constitution §XI).
- [ ] **T012** `[BLOCKED: G2]` `[OWNER]` Owner **both-sides G2 approval** of the
  contract → this is what flips G2 to satisfied and unblocks children. `[OWNER]`.

---

## Phase C — G3 schema slice `[GATED]` (after T010/T012)

- [ ] **T020** `[BLOCKED: G3]` `[BLOCKED: G2]` Promote the conceptual model (spec §9) to a
  `[GATED]` migration: payer-account / receivable / payment-application / claim /
  remittance + RLS + idempotency keys + audit columns. No table/column names committed
  until this slice. `[GATED]` path `packages/db/**`.

---

## Phase D — Service + worker slices (after T010/T020)

- [ ] **T030** `[BLOCKED: G2]` `[BLOCKED: G3]` Receivable lifecycle state machine +
  settlement-state projection over the immutable sale (FR-005..FR-008); replay-safe.
- [ ] **T031** `[BLOCKED: G2]` `[BLOCKED: G3]` `[BLOCKED: OQ-7]` Payment / cash-application
  service (FR-011, FR-012); idempotent; audited.
- [ ] **T032** `[BLOCKED: G2]` `[BLOCKED: G3]` Claim/remittance reconciliation worker
  (FR-014); variance recording; rejection routes to DP-026 reuse (FR-015, NG-1).
- [ ] **T033** `[BLOCKED: G2]` `[BLOCKED: G3]` Authorization wiring: POS settlement-intent
  via 028-arc operator envelope; Console via session; safe-404 isolation (FR-019, §8).
- [ ] **T034** `[BLOCKED: G2]` `[BLOCKED: G3]` Observability signals for settlement /
  receivable health (FR-021, §7); metric names defined here.

---

## Phase E — Children consume (separate repos; NOT authored by DP-2)

> Consumer notes only — DP-2 does not author these (spec NG-2). Listed for sequence.

- [ ] **T040** `[BLOCKED: G2]` POS 020 — pos-credit-and-third-party-tender-flow (consumes
  settlement-intent contract).
- [ ] **T041** `[BLOCKED: G2]` Console 017 — customer-and-payer-accounts.
- [ ] **T042** `[BLOCKED: G2]` Console 018 — receivables-and-insurance-claims.
- [ ] **T043** `[BLOCKED: G2]` Connector 009 — receivables-and-third-party-posting-adapter
  (later consumer; posts approved commands).
- [ ] **T044** `[BLOCKED: G2]` **Console 019 — settlement-reconciliation (LAST)**; also
  needs **DP-2 032 runtime wiring** before end-to-end.

---

## Dependency summary

```
T001(CARVE)✓ ┐
T002(7-C)  ✓ ┴─► T010a(draft)✓~ ─► T010b(OpenAPI)[GATED] ─► T012(G2 approval) ─► T020(G3 schema) ─► T030..T034
                                          │                       │
                                          └───────────────────────┴─► T040..T044 (children, other repos)
T003(tax-off)✓ ··· tax-pending; does not block non-tax work
[reversal fields / FR-024] ···· BLOCKED on DP-026 close → later additive bump (not in this contract)
```

## Status honesty

- T001/T002/T003 **RESOLVED** by signed owner ruling (035-DR-SETTLEMENT); this cleared
  the **non-reversal** G2 contract **draft** (T010a) only.
- **Reversal-compatibility fields (FR-024) remain genuinely BLOCKED** on DP-026's close
  — carve is sequencing, not a compat answer.
- The draft (T010a) is **pre-G2 design input, revisable**; it does **not** flip G2.
  G2 needs the authored OpenAPI contract (T010b, `[GATED]`) + owner both-sides approval.
- The original two blockers were **T001 (OQ-4)** and **T002 (OQ-7)** — both `[OWNER]` /
  cross-system decisions that repo evidence cannot settle.
- No table/endpoint/field/migration is named; G2/G3 remain unsatisfied.
- Uncommitted; for review.

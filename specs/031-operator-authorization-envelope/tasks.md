# Tasks — D1+D2 DP-2 Operator-Authorization Envelope (Mint, Return & Re-wire)

> **PLAN-ALTITUDE TASK LIST.** Authored after owner ratification of the OQs (spec.md Clarifications 2026-06-12) and the merged [`plan.md`](./plan.md). **No code, SQL, contract, or test is authored here** — this is an ordered task list the owning repo would carry out under its own review. Execution (any task below touching real files) requires a **further** explicit, scoped owner approval after **G10 dispatch-clearance** (an Orchestrator-side check; the boundary is signed = planning precondition only).

**For:** Data-Pulse-2 to execute **POST-dispatch**.  **Date:** 2026-06-12.  **Decider:** Owner (Ahmed Shaaban).

**Gate legend:** **G10** = Identity & Access Boundary (blocks all D1/D2 work until dispatch-clearance) · **G2** = Contract Gate (the OQ-5 authz scheme + sale-route re-wire in `pos-sales/sales.yaml`) · **no G3** = no Migration Gate (OQ-1=1-A-i reuses the existing `auth_tokens` row; zero schema change — confirm survives T4's chosen mechanism).

---

| # | Task | Gates | Depends on | Notes |
|---|---|---|---|---|
| **T0** | **Gate + state pre-flight.** Verify G10 dispatch-clearance is granted (boundary signed = precondition; clearance = the Orchestrator check). Verify DP-2 `origin/main` clean; re-read `pos-operators.service.ts` (`issueOperatorSessionRow`, sign-out/takeover revoke), `auth.guard.ts` (`findActiveByRawToken`, `principalFromToken`, `BEARER_AUTH_SCOPES`), `pos-operator-auth.guard.ts`, `pos-operator-sale-auth.guard.ts`, `operator-context-resolver.ts`, `catalog/sales/sales.controller.ts`. | G10 | — | **Hard stop** if G10 dispatch-clearance is open or state drifted from the plan's evidence basis. |
| **T1** | **Return the envelope at sign-in (OQ-1 = 1-A-i).** In `issueOperatorSessionRow`, stop discarding the `generateRawToken()` value; return it. `token_hash` + the `auth_tokens` row unchanged (**no schema change → no G3**). | G10 | T0 | Closes D1 issuance half (G-1). Raw never logged/audited (G-7). |
| **T2** | **Extend the sign-in + takeover-confirm response DTO** to carry the envelope alongside `{ id, issued_at }` (`PosOperatorSessionSummaryBody` / `PosOperatorSignInResponseBody`), **and document the new response field in `pos-operators.openapi.yaml`** (a contract change → **G2**, in a *different* contract file from T7's `sales.yaml`). Field name MUST match T7. | **G2**, G10 | T1 | Realizes A-1. **Contract-first ordering:** the contract field (this row) must land with/before the code returns it; do not let the DTO precede its contract by the full T7 gap. |
| **T3** | **Re-wire `captureSale` / `recordVoid` / `recordRefund`** from `@UseGuards(PosOperatorSaleAuthGuard)` (Option-Y) onto the canonical `PosOperatorAuthGuard` (`scope==="pos_operator"`) — the guard the returned envelope now satisfies. | G10 | T1 | Closes D2 (phantom) + D1 use half (G-2, G-3, A-2, A-3). |
| **T4** | **DECISION TASK — choose the G-4 live-predicate mechanism (Phase 2.5 A vs B).** Determine whether **028 §9 revocation reconciliation** covers all three axes (membership / device / store-access) propagating to the envelope's `auth_tokens` row. **If yes → Option A** (revoke the envelope row on revocation; wire it or declare the hard 028 §9 dependency). **If no/uncertain → Option B** (re-resolve membership/device/store-access per request at the sale-route guard). Record the choice + rationale in plan.md/this row before T5. | G10 | T0 | **Blocks T5.** Carried open from plan Phase 2.5. **Do NOT invent** — this is a real fork gated on 028 §9 coverage. Verified this session: **no** revocation→`auth_tokens` propagation exists today. |
| **T5** | **Implement the chosen G-4 mechanism (from T4).** *Option A:* on membership-revoke / device-revoke / store-access-pull, `UPDATE auth_tokens SET revoked_at = now()` for the affected operator's live envelope row(s) (reuses sign-out's mechanism — **still no schema change**). *Option B:* the per-request re-resolution guard/wrapper restoring Option-Y's live checks behind the envelope credential. | G10 | T4, T3 | Upholds G-4 / A-4 — the envelope must **not weaken** live revocation vs Option-Y. **Re-confirm no-G3 holds** for the chosen path; if a new column/index is needed, **flip G3 on and surface** (do not absorb). |
| **T6** | **Retire `PosOperatorSaleAuthGuard` for the sale routes.** Confirm no non-sale consumer; detach from the three routes; delete only if no remaining consumers. **No parallel Option-Y path retained.** | G10 | T3, T5 | A-3 (no dual path → no DOC-3 mismatch). |
| **T7** | **Contract (OQ-5) — new authz scheme + sale-route re-wire.** Introduce a **new distinct** authorization-credential security scheme (e.g. `operator-authorization` / `pos-operator-envelope`, opaque `pos_operator` bearer, `Authorization: Bearer`) in the OpenAPI contracts; apply it to the three sale routes in `pos-sales/sales.yaml`, replacing `clerkJwt`. **MUST NOT reuse 030's `operator-identity`** (identity-proof-only). `operationId`s unchanged (no breaking rename). *(The sign-in/takeover response envelope field is documented in `pos-operators.openapi.yaml` under T2 — the **second** G2 surface; keep the field name consistent across both files.)* | **G2**, G10 | T6 | DOC-3 co-travel (028 §19). Conformance tests gate both contract files. |
| **T8** | **Test pass.** Unit (sign-in returns non-empty envelope; envelope→`findActiveByRawToken`→`pos_operator` principal satisfies the canonical guard; sign-out `revoked_at` invalidates it). Integration RLS-aware (capture/void/refund via envelope succeed with same membership/store/eligibility outcomes; device-only / provider-JWT-only rejected; provenance unchanged). Regression (Option-Y off the three routes, no parallel path; `readSale` guard unchanged but now satisfiable). Contract conformance (new scheme present, applied, NOT `operator-identity`; response documents the envelope). Negative/non-disclosure (missing/invalid/revoked/expired → same generic 401; no envelope material in logs). | G10 (+G2 for contract tests) | T5, T6, T7 | Proves A-1…A-7. |
| **T9** | **G-4 live-predicate regression test (the Phase 2.5 guard).** After a valid envelope is issued, revoking the operator's **membership**, **device**, or **store-access** mid-session **stops further sales** (next capture/void/refund fails). **MUST fail** against a naive re-wire that omits T5; **MUST pass** once the chosen mechanism is in place. | G10 | T5, T8 | The explicit guard for G-4 / A-4 — the load-bearing risk of the slice. |
| **T10** | **Slice verification + return to Orchestrator.** Confirm: envelope returned + satisfies the canonical guard; the three sale routes on the canonical path; Option-Y retired; composed predicate (incl. live revocation, T9) holds; provenance/audit intact; new authz scheme ≠ `operator-identity`; resolution join key unchanged (D3 untouched). Return to the Orchestrator for gate verification before any downstream slice (D5/D7) is dispatched. | G10, G2 | T8, T9 | Per CLAUDE.md: return to the orchestrator for gate verification before continuing cross-repo. |

## Dependency notes

- **Upstream:** D1/D2's only blocking gate is **G10** (boundary signed; dispatch-clearance pending). Recommended-first **D3 (029) is SHIPPED**, so its sequencing prerequisite is satisfied; D3 is not a hard gate on D1 (spec Dependencies).
- **The critical internal fork:** **T4 (decision) blocks T5 (implementation).** The G-4 mechanism (A: 028 §9 reconciliation vs B: re-resolve-at-guard) is **not** pre-decided — it is gated on whether 028 §9 guarantees coverage of all three revocation axes for the envelope. Resolving T4 may itself require an owner/028 confirmation.
- **Gate distribution:** G10 on **all** tasks; **G2** on **T2** (the `pos-operators.openapi.yaml` response field) **and T7** (the `pos-sales/sales.yaml` scheme + sale-route re-wire) — two contract files (+T8/T10 for their conformance); **no G3** anywhere — but T5 must **re-confirm** the no-schema-change claim survives the chosen mechanism and surface a flip to G3 if not. *(Contract-first note: T2's contract field should not lag its DTO code; keep them together.)*
- **Downstream (recorded, not executed here):** **D5** (POS adopts/presents the envelope) is hard-gated on D1; **D7** (device token reverts to device-scoped) follows D5; **D6** (POS offline-PIN re-anchor) needs D3 + the D1/D5 envelope carrying `user_id`.
- **Parallelizable:** T1 and the T4 decision-investigation can proceed together (T4 reads 028 §9, doesn't depend on T1). T7 (contract) can be drafted in parallel with T5 but lands after T6.

---

> **No code authored here.** Each task is executed inside Data-Pulse-2 under that repo's review only after a **further** explicit, scoped owner approval and verified **G10 dispatch-clearance**. No migration SQL, TypeScript, or contract YAML is produced by this document. `T4` is a genuine open decision — it must be resolved (not assumed) before `T5`.

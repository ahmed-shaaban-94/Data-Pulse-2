# Active Feature Log — historical narrative

> **Extracted from `CLAUDE.md` (2026-06-12) to reduce always-loaded context overhead.**
> This is the full arc-history narrative that used to live under `## Active feature` +
> `## Specs summary` in the project `CLAUDE.md`. It is **reference-on-demand**, NOT loaded
> every turn. **For slice-level state, the source of truth is each spec's
> `execution-map.yaml` + `wave-status.md`** — read those, not this log. This file may lag.
>
> `CLAUDE.md` keeps only a compact current-state pointer; consult this log when you need the
> full multi-spec history.

## Active feature

**ERPNext integration arc (011→018): 011→018 all SHIPPED on `main`; 017 + 018 CLOSED.** The arc is functionally complete for everything buildable in this repo — the remaining frontier is **cross-system live validation** (connector repo + a staging ERPNext), which is external. The foundation + first contract shipped: **011-erpnext-pos-reference-and-integration-foundation** CLOSED (PRs #468 + #472) — docs-only foundation + four SIGNED decision records (posting / stock-impact / tax-fiscal-egypt / version-pin); **ADR 0008** (connector repo split) ACCEPTED + merged (#479); **012-erpnext-connector-contracts** planning merged (#476); and the **`[GATED]` 012-CONTRACT** DP2↔connector OpenAPI contract is **SHIPPED** — `packages/contracts/openapi/erpnext-connector/posting-feed.yaml` + its 28/28 conformance spec, merged via **PR #481 (`aad0cf9`)**. The contract defines a **pull/feed bidirectional** transport: `connectorPullPostings` (GET cursor feed of pending postings) + `connectorAckOutcome` (POST outcome ack; requires `Idempotency-Key`), auth via an opaque **machine** `connectorBearer` scheme (NOT POS `clerkJwt`), namespace `/api/connector/v1/erpnext`, work-item mirrors the 008 sale projection, **Payment Entry deferred** (008 has no tender, gate A.5). The connector itself lives in a separate repo (`Retail-Tower-ERP-Next-Connector`) — **now a real, actively-built Frappe app** (specs 001–006, Connector Settings, `dp2_token`, posting adapter, poller activation merged connector-PR #23), no longer LICENSE+README only. **Since then the whole DP2 side of the arc shipped: 013 (product master, item-map) CLOSED, 014 (warehouse map) CLOSED, 015 (POS-sale→ERPNext posting feed/ack/reversal) CLOSED, 017 (reconciliation & repair) CLOSED.** **018-connector-boundary-hardening is CLOSED on `main`** (planning chain PR #514; implementation 10 slices via PR #516, 2026-06-07): a DP2-owned pilot-safety boundary — connector instance identity (`[GATED]` `0021` `connector_registration` + `auth_tokens.connector_registration_id` FK + at-most-one-active partial-unique), credential issue/rotate/revoke/disable lifecycle (raw secret once; reuses opaque-bearer, no new primitive), a tightened `ConnectorAuthGuard` (full registration-link usability predicate + identity attach + non-disclosing 401; generic dashboard/POS path untouched), a session-only admin guard (FR-005c: human cookie only, rejects `dashboard_api` bearer) + `RolesGuard`, `[GATED]` `connector-admin.yaml` (6 ops, cookieAuth), `connector_lifecycle_total` signal, and the boundary-of-record doc. Tests: connector 95/95 + auth/015-posting regression 502/502 + worker-obs 232/232. **Two carried follow-ups (gated on a live env, NOT blockers):** (1) the `auth_tokens` consistency CHECK (`scope='connector' iff connector_registration_id IS NOT NULL`) is DEFERRED — owner confirmed a legacy unlinked connector token may exist; FK + partial-unique ship now, the CHECK lands after a live backfill; (2) US4 release-runbook — a pre-existing unlinked connector token is rejected by the tightened guard, so before US4 reaches a live env: backfill a registration → link/reissue → reconfigure the connector. Next in the arc is otherwise external/gated: **ERPNext-major staging validation → the cross-system live legs** (the connector poller actually posting to / reading from a real ERPNext). 016 (tax/fiscal-egypt) is on-hold (owner, 2026-06-06; its hard decisions are SIGNED in `011-DR-TAX-FISCAL-EG`; plan if/when needed). Future arc handoffs named by 018: 019 (live ERPNext-Bin stock-view contract = the 017-deferred `017-STOCK-VIEW-CONTRACT`), 020 (connector health/status), 023 (sales-posting command, if needed), 029 (scheduled reconciliation). Planning chains for the 019/020/021/023/025 wave MERGED to `main` 2026-06-07 via PR #525 (squash `75d9967`). **019 has since advanced far beyond planning: `[GATED]` CONTRACT (PR #526 `88562af`) + T040 bin-view feed/report runtime + T041 017-rewire (`ReportBackedBinView` replaces `EMPTY_BIN_VIEW`) MERGED via PR #528 (`9a3f475`, 2026-06-08); connector bin-view client merged (connector PR #25 `ce44129`); cross-tenant idempotency fix merged (PR #532 `b08591c`, closes issue #530); full loop LIVE-VALIDATED 2026-06-08 (empty-Bin → completed `{}`; ERPNext qty 10 / DP2 on-hand 10 → `{match: 1}`). Open follow-ups: #529 (OTel SDK boot hang, env-guard needed), #531 (019 multi-window for >500-item warehouses).** **020 (connector health/status) and 021 (product-master reconciliation) SHIPPED via PR #534 (`6dac49f`, 2026-06-08):** 020 — `[GATED]` migration `0022` `connector_health` table (FK→`connector_registration`, fail-closed RLS, no DELETE, NO PII/money) + `[GATED]` `connector-health.yaml` contract; heartbeat (`connectorBearer`) + operator read (human `cookieAuth`, session-only 018 guard); pure `deriveLiveness` verdict (disabled→never_seen→healthy→stale, 5-min); `connector_heartbeat_total` signal; deferrals non-blocking: proactive dark-detection / scheduled stale-sweep (#523). 021 — `[GATED]` migration `0023` (run + result + append-only repair_attempt tables, fail-closed RLS, no DELETE) + `[GATED]` `product-reconciliation.yaml` contract; US1 unmapped-product backlog read; US2 repair via 013's existing suggest/confirm/re-point lifecycle (READ-NOT-MUTATE §IX — no direct `erpnext_item_map` write); US3 stub-tolerant reconciliation run (connector seam + EMPTY stub, no outbound ERPNext HTTP); `erpnext_product_reconciliation_total` signal; deferrals non-blocking: live ERPNext-item read + `attribute_drift` emission (`021-ITEM-VIEW-CONTRACT`, epic #524). The indicative migration-number collision (#520) is **resolved by authoring order** — 020 took `0022`, 021 took `0023`. **025 (console sync-ops read-model) SHIPPED via PR #527 + closeout PR #535:** `[GATED]` `console-sync-ops.yaml` contract + `erpnext-sync-ops` api sub-module; 3 compute-on-read GET verticals over 015/017 data; 42/42 green; cookieAuth human-only; the 020/021 DomainSummary domains correctly report `not_available` (forward-compat stub) — a future additive slice wires them now that 020/021 have landed. **023 is explicitly PLAN-ONLY — implement only if a concrete need is confirmed (its T005 owner gate, issue #521).** See the Specs-summary entries below + [specs/012-erpnext-connector-contracts/wave-status.md](../../specs/012-erpnext-connector-contracts/wave-status.md). The 008→012 chain in `docs/ROADMAP-ERP.md` is superseded for 011+ (erratum there points to 011).

**028 auth-boundary follow-up arc (D3/D4) — 029 + 030 SHIPPED 2026-06-12.** Independent of the ERPNext arc. **029-dp2-provider-neutral-identity-link** (PR #550 `0af392e`): `[GATED]` migration `0025_external_identity_links` + `IdentityProviderPort` + resolver PI-3 re-point + clerk→link backfill; `users.clerk_user_id` demoted (not dropped); new-user live-link provisioning deferred (owner-accepted). **030-auth-contract-cleanup** (PR #551 `33515a6`): additive `device` + `operator-identity` security schemes in OpenAPI contracts; three surfaces kept on `clerkJwt` pending D1. **031-operator-authorization-envelope (D1+D2, the keystone) SHIPPED 2026-06-12** (PR #559 `202d253`) — see the spec's `wave-status.md`. **032-pos-sale-capture-sync-status-and-idempotency-contract MVP + US4 SHIPPED 2026-06-12** (PRs #553 `00c1539` + #561 `616af95`) — see its `wave-status.md`.

**`017-erpnext-reconciliation-and-repair` is CLOSED on `main`** (8/8 v1 slices merged 2026-06-06 via PR #509; the live-wiring follow-up `017-RECON-WIRING` merged via PR #511; DP2-side verification recorded via `017-VERIFY` PR #510). 017 is the ERPNext arc's **operational reconciliation surface** (011→017): **run → report → repair**. It makes the 015 posting dead-letter backlog **visible** (US1 🎯 — a read-projection over `erpnext_posting_status WHERE status='permanently_rejected'`, never a mirror — READ-NOT-MIRROR-015), exposes an **idempotent repair** that re-uses the 015 O-3 state machine (re-head a `permanently_rejected` row to `pending`; the connector re-posts via the **existing** 012 feed/ack → exactly one `document_ref`, never a 2nd, never a silent rewrite — REPAIR-REUSES-015-O3), and runs **stock reconciliation** comparing DP2 on-hand (009) against the connector's ERPNext-Bin view per the 014 mapping, persisting mismatch reports in 014's vocabulary (US3). New `[GATED]` `0020` `erpnext_reconciliation_*` table family (run + result + append-only repair_attempt; fail-closed RLS; NO money/PII); new `[GATED]` `reconciliation.yaml` operator contract (6 operationIds, **cookieAuth/DashboardAuthGuard human-only**, NOT connectorBearer; `/api/v1/catalog/erpnext-reconciliation`). **DP2 makes NO outbound ERPNext HTTP**; the 008 sale fact + 009 ledger are NEVER mutated (§IX). **`017-RECON-WIRING` (#511) resolved the live trigger→queue→processor deferral via the OUTBOX path** — registered `erpnext.reconciliation.requested` in the `[GATED]` `OUTBOX_EVENT_TYPES`, `triggerRun` emits it **in-transaction**, and `ReconciliationRequestedConsumer` (in `worker.module`'s drainer seam) invokes the run processor over `EMPTY_BIN_VIEW`: a triggered run now advances `running → completed` **DP2-internally** (no connector/ERPNext call). **Deferred (NOT 017 v1):** ~~the **live ERPNext-Bin read** (`017-STOCK-VIEW-CONTRACT`)~~ **RESOLVED by 019** (T041, PR #528 — `ReportBackedBinView` replaced `EMPTY_BIN_VIEW`; loop LIVE-VALIDATED 2026-06-08) + **scheduled runs** (`017-SCHEDULED-RUNS` — later wiring over the same processor) + perf report-only. [specs/017-erpnext-reconciliation-and-repair/](../../specs/017-erpnext-reconciliation-and-repair/)

**`010-pos-catalog-read-down-sync` is CLOSED on `main`** (10/10 slices merged 2026-06-04, PRs #460 + #462 + #463 + #469 + #473 + #475 + #477 + #478 + CLOSEOUT). The platform-side **read-DOWN** catalogue publication API ships: device-authenticated POS terminals pull the **Resolved Sellable Store Catalogue** (Tenant ⊕ Store Override, 003 §6.4, sellable-filtered, decimal money) as a cursor-paginated **snapshot** (`posGetCatalogSnapshot`) and advance via idempotent, gap-detectable **deltas** (`posGetCatalogDeltas`) — the opposite direction of 005 (capture-UP). Read-only (`clerkJwt` device-auth, no write surface). Backed by the **`0015` catalogue change-log** + 3 dumb DB triggers (R9: single global IDENTITY sequence + `store_id IS NULL` tenant-wide sentinels + `ON DELETE CASCADE` FKs because the change-log is a derived projection). The **stored op is ADVISORY** — the delta read re-resolves Tenant ⊕ Override per `(tenant, store)` and derives the wire op from current sellability (override-masking is a harmless idempotent re-upsert). Resolver sets `app.current_store` (store-axis RLS on `store_product_overrides`); money emitted at natural minor precision (`normalizeAmount`, never float); `catalog_unpriced_issue_rate` signal in the shared `api.metrics.ts`. **POS-Pulse 010 is UNBLOCKED** (`packages/contracts/openapi/catalog/read-down.yaml` pinned + snapshot/delta reachable, spec §10). Key in-build corrections: the contracts/README's `posDeviceAuth` was a mislabel (the device scheme is `clerkJwt`); the change-log FKs had to flip RESTRICT→CASCADE (a `RESTRICT` derived-projection row deadlocked real catalog deletions). Perf report-only (no perf env); FR-060 ETag + 003 PQ-6 signing deferred. [specs/010-pos-catalog-read-down-sync/](../../specs/010-pos-catalog-read-down-sync/)

**`009-inventory-stock-ledger` is CLOSED on `main`** (15/15 slices merged 2026-06-03, PRs #437–#444 + #451 + #454/#455/#456/#458/#459/#461/#464). The full append-only stock ledger ships: compute-on-read on-hand, manual/sale-linked/transfer/count/restock movements, the negative-balance allow-and-flag counter, and the §XIV data-class guard. **The two `[GATED]` `packages/db` follow-ups (issue #465) are now RESOLVED + merged:** the established-unit DB guard (PR #470, migration `0016` EXCLUDE constraint — closes the FR-022 concurrency gap) + the movement outbox emit (PR #471, `inventory.movement.created`). `008-sales-transaction-capture` is **CLOSED** (2026-05-31, 16/16 slices, PRs #420–#435). Confirm current state via GitHub (open PRs, recent commits) before acting. Recent shipped work and documented partials:

- **`009-CI-OPT`** — hosted-CI reliability infra slice (owner-directed 2026-06-01, `[GATED]` `.github/**`). After the CI runner was switched self-hosted → GitHub-hosted `ubuntu-latest` (PR #446 — the self-hosted runner was **dead**, so `db-integration` had silently never completed across 009), the hosted lane surfaced accumulated breakage: stale migration-count assertion (PR #447), self-referential barrel import (PR #448), then a **25-min job timeout** (api RLS+coverage step ran ~21 min, job killed twice). `009-CI-OPT` (PR #449) bumps `db-integration` `timeout-minutes` 25→40 and drops `--coverage` from the blocking api step (dominant cost; suite still runs + gates; coverage **deferred** to local/manual — wave-status finding #8). **`main` has NO branch protection — CI is advisory; verify each PR's `db-integration` manually before merge.** US3–US6 stay parked until a **green hosted canary** lands. [specs/009-inventory-stock-ledger/](../../specs/009-inventory-stock-ledger/)

- **`009-inventory-stock-ledger`** — append-only stock-movement ledger with compute-on-read on-hand, **decoupled** from 008's gated live loop (the backfill reads CAPTURED sale rows; never subscribes to `sale.captured`). **CLOSED 2026-06-03 — 15/15 slices merged** (PRs #437–#444, #451, #454/#455/#456/#458/#459/#461/#464): SIGNOFF, SETUP, `[GATED]` CONTRACT + `[GATED]` SCHEMA (`0014_inventory`), ISOLATION-HARNESS, US1-ONHAND 🎯, US2-MANUAL, US3-IDEMPOTENCY, US4-SALELINKED (service + worker backfill), US5-TRANSFER, US6-COUNT, SIGNAL-NEGBAL, RESTOCK, LIFECYCLE (§XIV guard), POLISH (closeout). **The two `[GATED]` `packages/db` follow-ups (issue #465, now CLOSED) are RESOLVED + merged:** established-unit DB guard (PR #470, migration `0016` EXCLUDE constraint, FR-022) + movement outbox emit (PR #471, `inventory.movement.created`). See the Specs-summary entry below + the spec's `execution-map.yaml`/`wave-status.md` for full slice detail. [specs/009-inventory-stock-ledger/](../../specs/009-inventory-stock-ledger/)
- **`008-sales-transaction-capture`** — the FIRST immutable sale fact the SaaS owns (`sales` + `sale_lines` + void/refund terminal events). **CLOSED 2026-05-31**, all 16 slices merged: schema + OpenAPI contract (`[GATED]`), isolation harness, US1 capture, US2 delayed-sync, US3/US4 void/refund, US5 idempotency, US6 safety, LIFECYCLE (SI-012 data-class guard), WORKER (off-request `processedAt` + advisory mismatch processor), WIRING (consumer-side BullMQ registration + no-unbounded-path guard), POLISH (report-only k6 perf + ≥80% coverage), CLOSEOUT. **THE `DP-008-LIVELOOP` FOLLOW-UP IS NOW LIVE (PRs #496 + #497, merged 2026-06-05)** — deferral (1) below is **RESOLVED**: the live capture→process loop runs end-to-end. Slice 1 (#496) registered `sale.captured` in `OUTBOX_EVENT_TYPES` (the gated `packages/db` half) + the `SaleCapturedConsumer` (deterministic `jobId` dedupe for at-least-once); slice 2 (#497) made `captureSale` emit `sale.captured` **in-transaction** (created path only, atomic with the insert — `SALES_OUTBOX_PRODUCER` is now dead seam code) + added the imperative `SaleWorker.start()` in `apps/worker/src/main.ts` (so `SaleWorker` IS now self-started by design — do NOT revert this) + a Docker-gated e2e proving capture→outbox→drain→consume→`processed_at` + idempotent re-run. **`processed_at` is now set off-request.** **TWO deferrals remain OPEN (not blockers):** (2) reconciliation-mismatch-rate signal emit (T092) — FR-031 *MAY*; §VII counter unregistered. (3) SC-010 perf report-only (no perf env; `loadtests/k6/sales-capture.js` ready). Plus a new **sale-processing metrics / DLQ / G7** follow-up flagged by #497 (resilience tail). [specs/008-sales-transaction-capture/](../../specs/008-sales-transaction-capture/)
- **`007-unknown-items-review-queue-api`** — Unknown Items Review Queue **API** (the dashboard-facing feature 006 deferred). Contract via #404; Wave 1 P1 MVP runtime via #405 (`0c1bec7`) + #406. **US7 reopen (#408), US8 bulk-dismiss (#409), and the US4/5/6 regression guards (#410) all subsequently MERGED** (2026-05-30) — the "dead CI runner" block from the interim handoff notes is resolved. Remaining 007 scope is polish/closeout per the spec's `execution-map.yaml`. [specs/007-unknown-items-review-queue-api/](../../specs/007-unknown-items-review-queue-api/)
- **`005-pos-catalog-sync-reconciliation`** — **FULLY CLOSED** 2026-05-29. Shipped 2026-05-23 to 2026-05-29. Waves 1 + 2 both COMPLETE on `main` (POS capture + reconciliation surface — link / create-product / conflict audit + metrics); auth-guard wiring complete on all five reconciliation routes (PRs #377 + #378). The deferred follow-up **`005-WAVE1-METRICS-MISMATCH-FOLLOWUP`** is now CLOSED — the multi-PR harness refactor (T532 + T550 + T551 + T552-mismatch-case) merged via PRs #389/#390, plus the T560 perf-flake preflight (#393/#394/#396) and full execution-map status reconciliation (#397, all 34 slices terminal with provenance). One interim deferral remains, tracked in `wave-status.md`: SC-008 p95/p99 perf assertions are report-only pending a dedicated perf environment (T560).
- **`006-unknown-items-review-queue`** — docs-only product brief; Claude-executable scope exhausted 2026-05-28 (10 of 23 slices closed via PRs #380, #381, #382, #383). Remaining 13 slices are reviewer-owned (T010–T019 per-US sign-offs) or external-gate-blocked on future API + UI feature specs that don't yet exist.
- **`004-platform-production-readiness`** — P7 exit-gate **PASS for exercised API/worker/outbox paths** as of 2026-05-21. P4 full signal-catalogue live-scrape coverage **PARTIAL** with explicit deferrals (DB pool, Redis, idempotency, auth-failure, RLS-failure, cross-tenant, suspicious-login signals not yet live-scraped). Authoritative status: [docs/production-readiness/004-closeout-status.md](../production-readiness/004-closeout-status.md). T483 partial documented in [docs/observability/operator-validation-report.md](../observability/operator-validation-report.md).

For slice state, always read the spec's `execution-map.yaml` and `wave-status.md` — do not rely on this file for task-level detail.

## Specs summary

- **`001-foundation-auth-tenant-store`** — shipped. Auth, tenant/store/memberships, audit pipeline, idempotency interceptor, outbox first slice all merged. [specs/001-foundation-auth-tenant-store/](../../specs/001-foundation-auth-tenant-store/)
- **`002-pos-operator-identity`** — specification + OpenAPI contracts only. POS app is a separate repo integrating exclusively via `packages/contracts/openapi/`. [specs/002-pos-operator-identity/](../../specs/002-pos-operator-identity/)
- **`003-catalog-foundation`** — complete. All 22 slices merged on main through PR #310. Schema modules, schema-shape tests, 5 gated SQL migrations (0007–0011), and Phase 3 RED+GREEN service-layer pairs all on main; all 5 findings resolved. [specs/003-catalog-foundation/](../../specs/003-catalog-foundation/)
- **`004-platform-production-readiness`** — P7 exit-gate PASS for exercised paths; P4 PARTIAL with explicit deferrals. See above and `004-closeout-status.md`.
- **`005-pos-catalog-sync-reconciliation`** — POS Catalog Sync + Reconciliation. **FULLY CLOSED** 2026-05-29 (shipped 2026-05-23 to 2026-05-29). Wave 1 (POS capture + dismiss + idempotency-mismatch audit + metrics) and Wave 2 (link + create-product + conflict + atomicity) both COMPLETE on `main`; auth-guard wiring complete on all five reconciliation routes (PRs #377 + #378). The `005-WAVE1-METRICS-MISMATCH-FOLLOWUP` harness refactor (T532 + T550 + T551 + T552-mismatch-case) is CLOSED via PRs #389/#390; T560 perf-flake preflight via #393/#394/#396; execution-map fully reconciled via #397 (all 34 slices terminal status with `merged_at_commit` provenance). Sole interim deferral: SC-008 perf assertions report-only pending a dedicated perf environment (T560). [specs/005-pos-catalog-sync-reconciliation/](../../specs/005-pos-catalog-sync-reconciliation/)
- **`006-unknown-items-review-queue`** — Unknown Items Review Queue, docs-only product brief. 10 user stories, 38 FRs, 9 SI requirements, 8 SCs. Spec/plan/Phase 0–1 artefacts + Agent OS coordination (T001–T023) merged on `main` via PRs #308 + #311; Claude-executable slice scope exhausted 2026-05-28 (10 of 23 closed via PRs #380, #381, #382, #383). Remaining 13 are reviewer-owned or external-gate-blocked. [specs/006-unknown-items-review-queue/](../../specs/006-unknown-items-review-queue/)
- **`007-unknown-items-review-queue-api`** — Unknown Items Review Queue **API** — the dashboard-facing feature 006 deferred, implementing over the shipped 005 surface. Planning chain + `[GATED]` OpenAPI contract extension (`ReviewQueueItem` projection, 3 new operationIds, `forbidden` 8th error code, list params) merged via PRs #400/#402/#404. **Wave 1 (Phase 2–5, P1 MVP) MERGED via PR #405 (`0c1bec7`)** + CodeRabbit follow-up #406 (`62d0906`): 7 slices — `forbidden`-category guard, shared `toReviewQueueItem` projection, isolation-harness terminal fixtures + sweep, review-safe list/dismiss/link/create projection swaps, list filter/sort/group extensions, and the new inspect `GET /{id}`. RED→GREEN, full catalog suite green (56 suites / 429 passed). All Wave-1 slices `status: merged`. **US7 reopen (#408), US8 bulk-dismiss (#409), and US4/5/6 regression guards (#410) subsequently MERGED 2026-05-30** (the interim "dead CI runner" block is resolved). Remaining: polish/closeout per the spec's `execution-map.yaml`. For slice state, read the spec's `execution-map.yaml` and `wave-status.md`. [specs/007-unknown-items-review-queue-api/](../../specs/007-unknown-items-review-queue-api/)
- **`008-sales-transaction-capture`** — first immutable sale fact (`sales` + `sale_lines` + void/refund terminal events), built over the shipped 005 POS ingestion seam (reuses Idempotency-Key interceptor, `sourceSystem+externalId` dedup, tenant-context/RLS, audit, outbox — no re-invention). **CLOSED 2026-05-31 — all 16 slices merged** (PRs #420–#435): SETUP/SIGNOFF, `[GATED]` SCHEMA (`0012` migration) + CONTRACT (`pos-sales/sales.yaml`), ISOLATION-HARNESS, US1–US6, LIFECYCLE, WORKER, WIRING, POLISH, CLOSEOUT. Money = string-backed value object (no `package.json` dep, gate A.6). **`DP-008-LIVELOOP` follow-up SHIPPED 2026-06-05 (PRs #496 + #497)** — the live capture→process loop is now functional: `sale.captured` registered in `OUTBOX_EVENT_TYPES` + `SaleCapturedConsumer` (#496), in-transaction emit from `captureSale` + `SaleWorker.start()` in `main.ts` (#497), Docker-gated e2e proving capture→`processed_at`. `processed_at` is now set off-request. **Two deferrals remain open** (wave-status Active findings): reconciliation-mismatch-rate emit (FR-031 *MAY*); SC-010 perf report-only — plus a new sale-processing metrics/DLQ/G7 resilience follow-up. Downstream reads of the sale fact (009 inventory, 012 reporting) are unblocked. [specs/008-sales-transaction-capture/](../../specs/008-sales-transaction-capture/)
- **`009-inventory-stock-ledger`** — append-only stock-movement ledger (`stock_movements` + `stock_counts`) with **compute-on-read** on-hand (signed SUM, no materialized balance), **decoupled** from 008's gated live loop (backfill reads CAPTURED rows; no `sale.captured` subscription). **CLOSED 2026-06-03 — 15/15 slices merged** (PRs #437–#444, #451, #454/#455/#456/#458/#459/#461/#464): SIGNOFF, SETUP, `[GATED]` CONTRACT + `[GATED]` SCHEMA (`0014_inventory`, RLS SELECT+INSERT-only, no version column), ISOLATION-HARNESS, US1-ONHAND 🎯 (compute-on-read on-hand + movement list), US2-MANUAL (`createStockMovement`; allow-and-flag negative FR-024; cross-unit reject FR-022; mass-assignment ban FR-052; ad-hoc nullable product no auto-create FR-023; audit-in-transaction), US3-IDEMPOTENCY (`@Idempotent` on the create route), US4-SALELINKED (`backfillSaleLinkedOutbound` service + `InventoryBackfillProcessor` worker; provenance dedup, decoupled R8), US5-TRANSFER (`createStockTransfer` — linked `transfer_out`/`transfer_in` sharing `transfer_group_id`; cross-tenant dest 404), US6-COUNT (`recordStockCount` — append-only `count_correction` for variance, no history rewrite), SIGNAL-NEGBAL (unlabeled `inventory_negative_balance_total` counter), RESTOCK (`backfillRestockInbound` — void/refund→inbound, the US4 mirror), LIFECYCLE (§XIV data-class guard), POLISH (k6 report-only perf + 500/req backfill ceiling + coverage). **The two `[GATED]` `packages/db` follow-ups (issue #465, now CLOSED) are RESOLVED + merged:** (1) the established-unit concurrency guard (PR #470, migration `0016` `stock_movements_one_unit_per_product` EXCLUDE USING gist — closes the FR-022 race where concurrent first-movements in different units could both commit); (2) the movement outbox emit (PR #471, `OUTBOX_EVENT_TYPES.INVENTORY_MOVEMENT_CREATED` emitted in-transaction from all write paths + the worker mirror, dedup-safe per FR-033). The `009-CI-OPT` infra slice made hosted CI reliable. [specs/009-inventory-stock-ledger/](../../specs/009-inventory-stock-ledger/)
- **`010-pos-catalog-read-down-sync`** — platform-side **read-DOWN** catalogue publication: device-authenticated POS terminals pull the **Resolved Sellable Store Catalogue** (Tenant ⊕ Store Override, 003 §6.4) as snapshot + delta, scoped to `(tenant_id, store_id)` (wire term `branch_id`). The opposite direction of 005 (capture-UP); read-only, `clerkJwt` device-auth, no write surface. **CLOSED 2026-06-04 — 10/10 slices merged** (PRs #460/#462/#463/#469/#473/#475/#477/#478 + CLOSEOUT): SIGNOFF-READONLY + SETUP (#460), `[GATED]` CONTRACT (`catalog/read-down.yaml` — `posGetCatalogSnapshot` + `posGetCatalogDeltas`, #463) + `[GATED]` SCHEMA (`0015` catalogue change-log + 3 dumb triggers, #462), ISOLATION-HARNESS (#469), US1-SNAPSHOT 🎯 (#473), US2-DELTA (#475), US3-ISOLATION (#477), POLISH (#478), CLOSEOUT. **Design:** the `0015` change-log is a derived projection — a **single global IDENTITY `sequence`** (monotonic-within-tenant, sparse per store — R9), tenant-wide `store_id IS NULL` **sentinel** rows, **dumb one-row-per-raw-change triggers** (no fan-out, no overrides consultation), and **`ON DELETE CASCADE` FKs** (a derived projection must never veto deletion of its subject — RESTRICT deadlocked real catalog deletes). The snapshot cursor **IS** the change-log head sequence (FR-011, so SCHEMA is FOUNDATIONAL). The **stored `op` is ADVISORY** — the delta read **re-resolves Tenant ⊕ Override per `(tenant, store)`** and derives the wire op from *current* sellability (override-masking = harmless idempotent re-upsert; handles override DELETE/deactivate). The resolver sets **`app.current_store`** (store-axis RLS on `store_product_overrides`) — not a SECURITY-DEFINER bypass; money emitted at **natural minor precision** (`normalizeAmount`, gate A.6, never float); excluded unpriced rows increment **`catalog_unpriced_issue_rate`** (registered in the shared `api.metrics.ts`, not a per-feature file). **POS-Pulse 010 is UNBLOCKED** (contract pinned + snapshot/delta reachable, spec §10). Key in-build corrections: the contracts/README's `posDeviceAuth` was a mislabel (device scheme = `clerkJwt`; manager = `cookieAuth`); change-log FKs flipped RESTRICT→CASCADE after the full api suite surfaced 17 deadlocked 005/007 teardowns. Perf report-only (no perf env); FR-060 ETag + 003 PQ-6 detached signing deferred. [specs/010-pos-catalog-read-down-sync/](../../specs/010-pos-catalog-read-down-sync/)

### 019–025 ERPNext-arc wave — planning MERGED 2026-06-07 (PR #525, squash `75d9967`); **019 SHIPPED + LIVE-VALIDATED 2026-06-08; 020+021+025 SHIPPED 2026-06-08**

The 018-named future arc handoffs now span both **shipped specs** (020/021/025) and **plan-only specs** (023). Planning chains for all five merged to `main` 2026-06-07 via PR #525 (squash `75d9967`). **020/021 SHIPPED via PR #534 (`6dac49f`, 2026-06-08); 025 SHIPPED via PR #527 + closeout PR #535 (`c3ece29`, 2026-06-08). 023 remains docs-only SpecKit planning only — no implementation, no gated artifacts authored.** Numbers 022/024 intentionally unassigned. Deferred work tracked as GitHub issues: **#524 (epic — ERPNext live-leg frontier, blocked on connector repo + staging ERPNext)**, #520 (**RESOLVED** — 020/021 migration collision resolved by authoring order: 020=`0022`, 021=`0023`), #521 (023 need-confirmation), #522 (025 source-name pinning — resolved by impl), #523 (020 proactive dark-detection).

- **`019-erpnext-stock-view-contract`** — **CLOSED + LIVE-VALIDATED 2026-06-08.** The 017-deferred `017-STOCK-VIEW-CONTRACT`. `[GATED]` CONTRACT (PR #526 `88562af`) + T040 bin-view feed/report runtime (`apps/api/src/catalog/erpnext-bin-view/`; `binViewPullRequests` + `binViewReportSnapshot`; **no new table**, run-scoped MERGE into `run.summary.bin_view_report`) + T041 017-rewire (`ReportBackedBinView` replaces `EMPTY_BIN_VIEW`; lifecycle shape-a: triggerRun WAITS on the feed for mapped stores; unmapped stores complete immediately) — all MERGED via PR #528 (`9a3f475`). Connector bin-view client merged connector-PR #25 (`ce44129`). Cross-tenant idempotency fix (PR #532 `b08591c`, closes #530). **Full loop LIVE-VALIDATED** against WSL ERPNext staging: empty-Bin → completed `counts {}`; ERPNext qty 10 / DP2 on-hand 10 → completed `counts {match: 1}`. Items keyed by `erpnextItemRef`, DP2-side-translated to `tenant_product_ref` via the confirmed 013 map. **NO `packages/db` table, no migration.** Open follow-ups: #529 (OTel SDK boot hang), #531 (multi-window for >500-item warehouses). [specs/019-erpnext-stock-view-contract/](../../specs/019-erpnext-stock-view-contract/)
- **`020-connector-health-and-connection-status-api`** — **SHIPPED via PR #534 (`6dac49f`, 2026-06-08).** Connector health / connection-status surface over 018's `connector_registration`. `[GATED]` migration `0022` `connector_health` table (FK→`connector_registration` ON DELETE CASCADE, UNIQUE per registration, fail-closed RLS SELECT/INSERT/UPDATE, no DELETE, NO money/PII — BUSINESS-class observational) + `[GATED]` `connector-health.yaml` contract. Heartbeat route (`connectorBearer` machine auth, LWW upsert, `last_seen_at = now()` server clock, NO per-beat audit row) + operator read routes (human `cookieAuth`, session-only 018 guard, non-disclosing 404). Pure `deriveLiveness` verdict (`disabled→never_seen→healthy→stale`, 5-min threshold). `connector_heartbeat_total` unlabeled counter (3-place register in `api.metrics.ts`). Tests: 49/49 across 6 suites (WSL Testcontainers). Deferrals non-blocking: proactive dark-detection / scheduled stale-sweep deferred to #523; migration collision with 021 resolved (020 took `0022`). [specs/020-connector-health-and-connection-status-api/](../../specs/020-connector-health-and-connection-status-api/)
- **`021-product-master-reconciliation-v1`** — **SHIPPED via PR #534 (`6dac49f`, 2026-06-08).** 017-style **run → report → repair** over 013's `erpnext_item_map` (the inverse of 014's stock recon, for products). **READ-NOT-MUTATE / not an authority handover (§IX)**; reuses 013's existing suggest/confirm/re-point lifecycle, owns no new mapping primitive; `cookieAuth/DashboardAuthGuard` human-only. `[GATED]` migration `0023` (run + result + append-only repair_attempt tables; fail-closed RLS; no DELETE) + `[GATED]` `product-reconciliation.yaml` contract (5 operationIds, `/api/v1/catalog/erpnext-product-reconciliation`). US1: live backlog read-projection (003 ⟕ 013 confirmed-only-and-active, no 021 table write). US2: repair drives 013's lifecycle + `repair_attempt` + in-tx `audit_events` atomically (FR-015); optional `runId`+`resultId` for persisted-result targeting. US3: stub-tolerant two-sided run (connector item-view seam + EMPTY stub; `erpnext_view_status='unavailable'` when absent; no outbound ERPNext HTTP). `erpnext_product_reconciliation_total` signal. Tests: schema-shape 12/12, contract 19/19, worker 161/161, migration 0023 17/17, HTTP 9/9, US1+US2 6/6. Deferrals non-blocking: live ERPNext-item read + `attribute_drift` emission deferred to `021-ITEM-VIEW-CONTRACT` (#524). [specs/021-product-master-reconciliation-v1/](../../specs/021-product-master-reconciliation-v1/)
- **`023-sales-posting-command-contract-v1`** — **PLAN-ONLY; implement in future ONLY if a concrete need is confirmed (its T005 owner gate).** A command-style (per-work-item, imperative) `[GATED]` DP2↔connector contract — an **additive, parallel, optional** alternative to the shipped 012/015 pull/feed transport (which already posts sales and is sufficient for the pilot). Reuses the 012 vocabulary verbatim (money, idempotency, error envelope, sale projection, Payment-Entry deferral gate A.5); `connectorBearer` machine auth; NO new schema (reuses 015 posting-status + 017 recon). **OQ-1 RESOLVED by owner 2026-06-07: connector-initiated** (DP2 stays the HTTP server; genuine DP2→connector push REJECTED — it would invert §IX and need its own decision record + spec). [specs/023-sales-posting-command-contract-v1/](../../specs/023-sales-posting-command-contract-v1/)
- **`025-console-sync-ops-read-model-v1`** — **CLOSED via PR #527 (`a3ccb4a`) + closeout PR #535 (`c3ece29`, 2026-06-08).** A **read-model / projection** for the Retail Tower Console (sibling admin-UI repo) aggregating the ERPNext sync-ops surface. **Compute-on-read, no persistence, no migration, no write surface**; `cookieAuth/DashboardAuthGuard` human-only. `[GATED]` `console-sync-ops.yaml` contract (3 read-only GET operationIds: `consoleGetSyncOpsSummary`, `consoleListPostingBacklog`, `consoleListReconciliationRuns`; `/api/v1/catalog/erpnext-sync-ops`) + `erpnext-sync-ops` api sub-module (service + controller + strict Zod DTOs). Populates **015** (posting-backlog) + **017** (reconciliation-runs) verticals; **020/021** DomainSummary domains report `not_available` (forward-compat stub) — a future additive slice wires those now that 020/021 have shipped. Tests: 42/42 across 4 suites (WSL Testcontainers). [specs/025-console-sync-ops-read-model-v1/](../../specs/025-console-sync-ops-read-model-v1/)

- **`029-dp2-provider-neutral-identity-link`** — provider-neutral identity link (028 drift D3). **CLOSED via PR #550 (`0af392e`), 2026-06-12.** `[GATED]` migration `0025_external_identity_links` (tenant-agnostic, NO RLS by design — resolver reads it before any tenant GUC exists); `IdentityProviderPort` + Clerk adapter; resolver PI-3 re-point to `external_identity_links` keyed on `(provider_key, subject)`; clerk→link backfill; `users.clerk_user_id` demoted to bridge column (NOT dropped; index + CHECK retained). Deferral (owner-accepted, non-blocking): new-user live-link provisioning lands in a future sign-in re-point slice. [specs/029-dp2-provider-neutral-identity-link/](../../specs/029-dp2-provider-neutral-identity-link/)
- **`030-auth-contract-cleanup`** — additive OpenAPI security-scheme cleanup (028 drift D4). **CLOSED via PR #551 (`33515a6`), 2026-06-12.** `device` scheme (no `bearerFormat: JWT`) applied to `catalog/read-down.yaml`; `operator-identity` scheme (JWT, identity-proof-only) applied to pos-operators/pos-shifts/vouchers. Three surfaces intentionally kept on `clerkJwt` with deferral notes: `pos-sales/sales.yaml` (Option-Y, deferred to D1/DOC-3), `catalog/unknown-items.yaml` (opaque pos_operator token), `pos-audit-events.openapi.yaml` (genuine Clerk JWT; device gate is a body attestation). No `service` scheme; connector/erpnext contracts untouched; no DB migration (contract-only change). [specs/030-auth-contract-cleanup/](../../specs/030-auth-contract-cleanup/)
- **`031-operator-authorization-envelope`** — the **keystone** 028 drift item (D1+D2: mint+return the operator-authorization envelope at sign-in **and** close the "phantom guard"). **SHIPPED 2026-06-12 (PR #559 `202d253`).** All 10 tasks (T0–T10): envelope minted+returned at sign-in (OQ-1 sub-fork 1-A-i — return the raw `generateRawToken()`, keep `auth_tokens` hash/revocation, no schema change), sale write routes re-wired onto the canonical envelope-bearer guard with the **T4=Option B** per-request live-revocation predicate (via `OperatorContextResolver`), Option-Y retired, two `[GATED]` contract surfaces (`pos-sales/sales.yaml` new `operatorAuthorization` scheme + `pos-operators.openapi.yaml` nullable `envelope`). **Option A** (028 §9 event-driven reconciliation) is the documented end-state. Downstream D5/D6/D7 are cross-repo (POS). [specs/031-operator-authorization-envelope/wave-status.md](../../specs/031-operator-authorization-envelope/wave-status.md)
- **`032-pos-sale-capture-sync-status-and-idempotency-contract`** — server-authoritative sale sync-status (§7) + Console read/repair surface (§9) + dead-letter classification (§8). **MVP + US4 SHIPPED 2026-06-12** (PRs #553 `00c1539` MVP + #561 `616af95` US4): `[GATED]` migration `0026_sale_sync_status` (`sales.sync_status` + `sale_sync_deadletters`), status vocab, US1 status binding, US2 read/repair surface (`sale-sync-ops.yaml`, 4 cookieAuth ops), US4 classifier + quarantine producer. **Deferred (non-blocking):** live drain-trigger wiring (producer is a tested capability not yet auto-fired), metrics emission, US5 422-path (GATED §13 item 1), `sales.yaml` §12 ops (GATED §13 item 4). [specs/032-pos-sale-capture-sync-status-and-idempotency-contract/wave-status.md](../../specs/032-pos-sale-capture-sync-status-and-idempotency-contract/wave-status.md)

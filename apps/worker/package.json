{
  "name": "@data-pulse-2/worker",
  "version": "0.0.0",
  "private": true,
  "description": "NestJS standalone + BullMQ workers (email, audit-fanout, session-revoke). Email processor + provider-agnostic adapter; queue/Redis bootstrap and provider wiring deferred (T090, T301, PQ-1).",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "jest --passWithNoTests",
    "lint": "echo \"worker: lint runs at the repo root\""
  },
  "dependencies": {
    "@nestjs/common": "11.1.19",
    "reflect-metadata": "0.2.2",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "@types/jest": "29.5.14",
    "@types/node": "20.17.6",
    "jest": "29.7.0",
    "ts-jest": "29.2.5",
    "typescript": "5.6.3"
  }
}
